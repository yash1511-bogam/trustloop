name: Deploy to Render

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy with cache clear
        run: curl -s "${{ secrets.RENDER_DEPLOY_HOOK_URL }}&clearCache=clear"
